---
- name: Step 000 - Pre Infrastructure
  hosts: localhost
  connection: local
  become: false
  gather_facts: false
  tasks:
    - name: Print debug message
      ansible.builtin.debug:
        msg: "Step 000 Pre Infrastructure (Troshka — no cloud pre-checks needed)"
