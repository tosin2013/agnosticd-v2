---
apiVersion: v1
kind: Service
metadata:
  annotations:
    metallb.universe.tf/allow-shared-ip: svc-{{ _cluster_name }}
  name: {{ svcname }}
  namespace: {{ ai_ocp_namespace }}
spec:
  ports:
  - name: https
    port: 443
    protocol: TCP
    targetPort: 443
  - name: http
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    role: "{{ role | default('worker') }}-{{ _cluster_name }}"
  type: LoadBalancer
